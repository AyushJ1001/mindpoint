// Shared Open Graph image. Every page that defines its own `openGraph` block
// replaces the layout's, so it must include this or the social preview image
// is dropped.
export const openGraphImage = {
  url: "/og-image.png",
  width: 1200,
  height: 630,
  alt: "The Mind Point — mental health education and professional development",
};
